let gameSeq = [];
let userSeq = [];

let started = false;

let level = 0;

document.addEventListener("keypress", function () {
    if (stareted == false) {
        console.log("Game stared");
        started = true;
    }
})
